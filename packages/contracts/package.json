{
  "name": "@papers/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "biome check src test package.json tsconfig.json",
    "test": "vitest run",
    "check": "tsc --project tsconfig.json --noEmit",
    "format": "biome format --write src test package.json tsconfig.json"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
